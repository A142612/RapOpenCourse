@EndUserText.label: 'Access control for ZI_RAP_Travel'
@MappingRole: true
define role ZI_RAP_TRAVEL_KEMIN {
    grant
        select
            on
                ZI_RAP_TRAVEL_KEMIN
                    where
  //                     ( TravelStatus )                       
 //                     = aspect pfcg_auth ( ZOSTATKEMI, ZOSTATKEMI,  actvt = '03') 
  //                      and
   //                     CurrencyCode = 'EUR'; 
                   true;
                        
}